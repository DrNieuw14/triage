requests>=2.31
pywebview>=5.0

# macOS's pywebview backend (WebKit via PyObjC) needs these; harmless/unused
# on Windows (which uses the built-in Edge WebView2 backend instead), but
# pip's environment markers keep them from even being considered there.
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
pyobjc-framework-WebKit>=10.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"

pyinstaller>=6.0
